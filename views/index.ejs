<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
    <title>messanger</title>
</head>
<body>
    <h1>All chats</h1>
    <form action="/chat/new" method="get">
        <button>New Chat</button>
    </form>
    <%for(let chat of chats){ %>
        <div class="chat">
            <p>From: <i><%= chat.from %></i></p>
            <div class="message">
                <p> <%= chat.message %></p>
                <form  method="GET" action="/chat/<%=chat._id%>/edit">
                    <button >Edit Chat</button>
                </form>
                <form method="post" action="/chat/<%=chat._id%>?_method=DELETE">
                    <button >Delete</button>
                </form>
            </div>
            
            <p>Receive by <i><%= chat.to %></i></p>
            
            <hr>
            <p><%= chat.time.toString().split(" ")[4] %><br>
            <%= chat.time.toString().split(" ").splice(0,4).join("-") %></p><br>
        </div>
        <br><br>
    <% } %>
    
    
</body>
</html>